# Optional dependencies (install only if you need the feature):
#   pip install -r requirements-optional.txt

# Real-time mic/webcam streaming in Streamlit (optional).
streamlit-webrtc>=0.47.0

# Audio extras (optional; useful for some voice pipelines).
torchaudio>=2.1.0

# Optional recommender alternative.
lightfm>=1.17

# Prefer full OpenCV instead of headless wheels (optional).
opencv-python>=4.8.0

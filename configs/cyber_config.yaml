# Cyber/intrusion domain override (UNSW-NB15)
project_name: "Sentifargo - Cyber"

data:
  # UNSW-NB15 binary label column
  target: label
  # Use the canonical labeled training split for stable local smoke runs.
  # This avoids scanning unrelated CSV files that may be huge/corrupt/unlabeled.
  path: data/raw/cyber/UNSW_NB15_training-set.csv
  # Avoid leakage: attack_cat is highly correlated with label
  drop_columns: [id, attack_cat]

training:
  model_type: hist_gb
  n_estimators: 300
  max_depth: 6
  learning_rate: 0.1
  max_iter: 200
  anomaly_contamination: 0.07

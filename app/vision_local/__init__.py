"""Local (offline) vision utilities.

This package provides lightweight, optional components for image understanding:
- embeddings (CLIP if available locally, otherwise deterministic fallbacks)
- simple object/category tagging
- small vector index helpers
"""

# DSA Evaluation Data

Replace the placeholder `DOC_ID_HERE` values in `qrels.jsonl` with real chunk IDs
from your indexed docs (`doc_id:page:chunk`). Then run:

```
python scripts/rag/evaluate_dsa.py
```

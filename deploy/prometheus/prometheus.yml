# Prometheus Configuration for OmniChatX
global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'omnichatx-monitor'

# Alerting rules
rule_files:
  # - "alerts/*.yml"

# Scrape configurations
scrape_configs:
  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # OmniChatX API metrics
  - job_name: 'omnichatx-api'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['omnichatx-api:8000']
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
        replacement: 'omnichatx-api'

  # Redis metrics (if using redis_exporter)
  - job_name: 'redis'
    static_configs:
      - targets: ['redis:6379']

# Alertmanager configuration (optional)
# alerting:
#   alertmanagers:
#     - static_configs:
#         - targets:
#           - alertmanager:9093

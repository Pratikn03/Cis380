# Sorting Overview

- Merge sort: stable, O(n log n), requires extra space.
- Quick sort: in-place, average O(n log n), worst O(n^2).
- Heap sort: in-place, O(n log n), not stable.

Pick based on stability needs, memory constraints, and data distribution.

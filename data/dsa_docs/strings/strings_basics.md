# String Algorithms Basics

- Pattern matching: naive O(n*m), KMP O(n+m).
- Two-pointer and sliding window are common for substring tasks.
- Hashing for substring checks (rolling hash).

Pitfalls:
- Unicode vs ASCII; be explicit about encoding.
- Off-by-one in window boundaries.

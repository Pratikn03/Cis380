# Sliding Window

Use sliding window to process contiguous subarrays in O(n) time.

Typical patterns:
- Fixed window: move start and end together.
- Variable window: expand end, then shrink start when condition holds.

Examples:
- Max sum subarray of size k.
- Smallest subarray with sum >= target.
- Longest substring with at most k distinct characters.
